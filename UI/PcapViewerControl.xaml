<UserControl x:Class="QuickLook.Plugin.PcapViewer.UI.PcapViewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="600"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Error overlay -->
        <Border x:Name="ErrorOverlay"
                Grid.RowSpan="3"
                Panel.ZIndex="10"
                Background="#80FFFFFF"
                Visibility="Collapsed">
            <TextBlock x:Name="ErrorMessage"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="18"
                       Foreground="#666666"
                       Text="No valid session found." />
        </Border>

        <!-- Top info bar -->
        <Border Grid.Row="0"
                Padding="12,8"
                Background="{DynamicResource CardBackground}">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="ProtocolIcon"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Text="?" />
                <TextBlock x:Name="InfoText"
                           Margin="8,0,0,0"
                           FontFamily="Consolas"
                           FontSize="14"
                           VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <Rectangle Grid.Row="1" Height="1" Fill="{DynamicResource CardBorderBrush}" />

        <!-- Bottom content -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TabControl x:Name="EncodingTabs"
                        Grid.Row="0"
                        SelectionChanged="EncodingTabs_SelectionChanged">
                <TabItem Header="UTF-8" />
                <TabItem Header="ASCII" />
                <TabItem Header="HexRaw" />
                <TabItem Header="HexDump" />
                <TabItem Header="GBK" />
            </TabControl>

            <!-- Single content area: FlowDocument with colored paragraphs -->
            <FlowDocumentScrollViewer x:Name="ContentViewer"
                                      Grid.Row="1"
                                      Margin="4"
                                      FontFamily="Consolas"
                                      FontSize="13"
                                      HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Auto"
                                      IsToolBarVisible="False" />
        </Grid>
    </Grid>
</UserControl>